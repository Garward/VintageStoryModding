<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <OutputPath>./</OutputPath>
    <AssemblyName>InterestingMeFix</AssemblyName>
    <RootNamespace>InterestingMeFix</RootNamespace>
    <Version>1.1.22</Version>
    <AssemblyVersion>$(Version)</AssemblyVersion>
    <FileVersion>$(Version)</FileVersion>
    <PackageFileName>$(AssemblyName)-v$(Version).zip</PackageFileName>
  </PropertyGroup>

  <PropertyGroup>
    <GameDirectory>/home/garward/Games/Games/VintageStory/vintagestory</GameDirectory>
    <PackageOutputPath Condition="'$(PackageOutputPath)' == ''">$(MSBuildProjectDirectory)/dist</PackageOutputPath>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="VintagestoryAPI">
      <HintPath>$(GameDirectory)/VintagestoryAPI.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="VintagestoryLib">
      <HintPath>$(GameDirectory)/VintagestoryLib.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="VSSurvivalMod">
      <HintPath>$(GameDirectory)/Mods/VSSurvivalMod.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="VSEssentials">
      <HintPath>$(GameDirectory)/Mods/VSEssentials.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="0Harmony">
      <HintPath>$(GameDirectory)/Lib/0Harmony.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Newtonsoft.Json">
      <HintPath>$(GameDirectory)/Lib/Newtonsoft.Json.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="interestingme">
      <HintPath>./lib/interestingme.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

  <Target Name="PackageDist" AfterTargets="Build" Condition="'$(Configuration)' == 'Release'">
    <MakeDir Directories="$(PackageOutputPath)" />
    <Delete Files="$(PackageOutputPath)/$(PackageFileName)" />
    <Exec Command="python3 -c &quot;import os, zipfile; root=r'$(MSBuildProjectDirectory)'; out=r'$(PackageOutputPath)/$(PackageFileName)'; z=zipfile.ZipFile(out,'w',zipfile.ZIP_DEFLATED); paths=[(r'$(TargetPath)','$(AssemblyName).dll'),(r'$(TargetDir)$(AssemblyName).pdb','$(AssemblyName).pdb'),(os.path.join(root,'modinfo.json'),'modinfo.json')]; [z.write(p,a) for p,a in paths if os.path.exists(p)]; assetroot=os.path.join(root,'assets'); [z.write(os.path.join(dp,f), os.path.relpath(os.path.join(dp,f), root)) for dp,_,fs in os.walk(assetroot) for f in fs] if os.path.isdir(assetroot) else None; z.close()&quot;" />
    <Message Text="Packaged: $(PackageOutputPath)/$(PackageFileName)" Importance="high" />
  </Target>
</Project>
